node_modules/
.DS_Store
debug/
scratch/

# Lokale scrape-output (working copy) is geen source — niet in git.
# Geankerd aan de root (/data/), anders matcht dit ook public/data/.
/data/

# public/data/shows.json is wél getrackt: de "Refresh data"-workflow commit
# dat bestand, zodat de deploy-workflow het zonder opnieuw te scrapen kan
# publiceren. Eventuele andere bestanden in public/data/ blijven genegeerd.
public/data/*
!public/data/shows.json
